# ios (placeholder)

Capacitor shell wrapping the `frontend/` Vue app, plus a native Swift card
scanner built on Apple's Vision framework, bridged to JS via a Capacitor
plugin. Scaffolded in a later slice (see root `CLAUDE.md` roadmap).
